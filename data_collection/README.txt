these are the data collection scripts, they use the "runEvolverFileParallel" file to call the surface evolver.
every file is for a different .fe file.
you need to set the file path for every .fe file and data collection folder. 
you can find the .fe files in their folder.
after the data collcetion is complete use the "convert_multiple_mat_to_table_database" to cllect it all to a single file.